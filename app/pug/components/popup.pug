+popup("popup-booked")
	.popup__steps 
		include ../blocks/popupStepBooked
	.popup__bottom
		.popup-actions
			button(type="button").btn.btn-blue Далее 
+popup("popup-booked-hall")
	.popup__steps 
		include ../blocks/popupStepBooked
		include ../blocks/popupStepHall
	.popup__bottom
		.popup-actions
			button(type="button").btn.btn-blue Далее 
+popup("popup-booked-calendar")
	.popup__steps 
		include ../blocks/popupStepBooked
		include ../blocks/popupStepHall
		.popup__step.step-popup.step-popup-calendar
			.step-popup__heading 
				.step-popup__title Выберите свободное время
			.step-popup__calendar.popup-calendar
				.popup-calendar__legends 
					.popup-calendar__legend.popup-calendar__legend--lock Занято
					.popup-calendar__legend Свободно
			#calendar
	.popup__bottom 
		.popup__bottom-quantity
			.popup__bottom-info Всего #[br] #[strong 1 заказ]
			.popup__bottom-info За один раз #[br] не больше 5-ти заказов
		.popup-actions
			.popup__bottom-price 
				| #[strong 10 000 ₽] 
				span включая скидку&nbsp;0 руб.
			+btnReserve
+popup("popup-services", "open-popup")
	.popup__step.step-popup.step-popup-services
		.step-popup__heading 
			.step-popup__title Дополнительные услуги
		.services-items 
			.services-item.services-item-card 
				.services-item-card__image.image-ibg
					img(src="images/booked/service-img.jpg", alt="карточка")
				.services-item-card__content 
					span Зал №2
					span 8-14 сентября
					span 06:00 - 12:00
			each service, index in bookedPage.services
				.services-item
					.services-item__header
						.checkbox 
							label.checkbox__label
								input(type="checkbox" name="brande" checked).real-checkbox
								span.custom-checkbox
								span= service.checkboxText
						.services-item__actions 
							.quantity-actions 
								button(type="button").quantity-btn
									<svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg">
										<path d="M4.1665 10H15.8332" stroke="#A4A4A4" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
									</svg>
								input(type="number", name="service_count" value=service.count).quantity-input
								button(type="button").quantity-btn
									<svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg">
										<path d="M10.0002 15.832L10.0002 4.16537M15.8335 9.9987L4.16683 9.9987" stroke="#307FF5" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
										<path d="M10.0002 15.832L10.0002 4.16537M15.8335 9.9987L4.16683 9.9987" stroke="black" stroke-opacity="0.2" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
									</svg>
							.services-item__price= service.price
					.services-item__content 
						p.
							В«Инструктаж по свету» входит: полноценная практика со студийным светом от нашего инструктора (и даже немного теории), -разбор нескольких базовых схем света и одной креативной. Внимание: инструктаж проводится только для одного человека, присутствие модели от фотостудии на съемке не предусмотрено, но можете пригласить свою модель
		.popup__bottom 
			button(type="button").btn.btn-border Назад 
			.popup-actions
				.popup__bottom-price 
					| #[strong 10 000 ₽] 
					span включая скидку&nbsp;0 руб.
				button(type="button").btn.btn-blue Продолжить 